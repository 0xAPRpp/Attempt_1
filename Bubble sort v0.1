# include <iostream>
using std::cin; //using std::cin;
using namespace std;//using namespace std
int main()
{
	const int size{ 10 };
	int arr[size];

	std::cout << "Enter exactly 10 array values: ";
	for (size_t i{}; i < size; i++)
		std::cin >> arr[i];
	bool swapped{true};
	while (swapped == true)
	{
		swapped = false; //This is the expectation - when all the elements are in ascending order - there is nothing to swap.
		for (size_t i{}; i < size-1; i++)
		{
			
			if (arr[i] > arr[i + 1])
			{
				int swap{};
				swap = arr[i];
				arr[i] = arr[i + 1];
				arr[i + 1] = swap;
				swapped = true; //This is set to true to ensure the condition is not met.
			}
		}
	}

	for (auto x : arr)
		std::cout << " " << x ;
	std::cout << std::endl;

	system("pause");
	return 0;
}
